version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build  # 这里指定构建输出目录
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
